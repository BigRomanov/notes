import { ShowImportantPipe } from './show-important.pipe';

describe('ShowImportantPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowImportantPipe();
    expect(pipe).toBeTruthy();
  });
});
